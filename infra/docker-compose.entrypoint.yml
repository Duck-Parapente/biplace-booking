services:
  entrypoint_caddy:
    image: caddy:2
    container_name: caddy-entrypoint
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile.entrypoint:/etc/caddy/Caddyfile:ro
    networks:
      - proxy

networks:
  proxy:
    external: true