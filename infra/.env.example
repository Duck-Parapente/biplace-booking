# Environment Configuration Example
# Copy this file to .env.local, .env.staging, or .env.prod and fill in your values

ENV=local

# Database Configuration (used by docker-compose)
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=your_database_name
POSTGRES_PORT=5432

# Docker/Caddy Configuration (used by docker-compose for staging/prod)
BACKEND_PORT=3001
HTTP_PORT=80
HTTPS_PORT=443

# Backend Configuration (passed to backend app via env_file)
DATABASE_URL=postgresql://user:password@host:port/database
NODE_ENV=development
PORT=3001
JWT_SECRET=your-very-secure-jwt-secret
LOG_LEVEL=debug
