# Production Environment
ENV=prod

# Database Configuration (used by docker-compose)
POSTGRES_USER=prod_user
POSTGRES_PASSWORD=prod_very_secure_password_change_me
POSTGRES_DB=biplace_prod
POSTGRES_PORT=5432

# Docker/Caddy Configuration (used by docker-compose)
BACKEND_PORT=3001
HTTP_PORT=80
HTTPS_PORT=443

# Backend Configuration (passed to backend app)
DATABASE_URL=postgresql://prod_user:prod_very_secure_password_change_me@postgres:5432/biplace_prod
NODE_ENV=production
PORT=3001
JWT_SECRET=production-jwt-secret-use-a-very-secure-random-string-here
LOG_LEVEL=error