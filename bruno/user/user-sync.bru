meta {
  name: user/sync
  type: http
  seq: 1
}

post {
  url: {{BACKEND_DOMAIN}}/user/sync
  body: json
  auth: inherit
}

headers {
  x-api-key: {{API_KEY}}
}

body:json {
  {
    "externalAuthId": "",
    "email": ""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
