meta {
  name: get-pack-reservations
  type: http
  seq: 2
}

get {
  url: {{BACKEND_DOMAIN}}/reservations/pack?packId={{PACK_ID}}
  body: none
  auth: bearer
}

params:query {
  packId: {{PACK_ID}}
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
