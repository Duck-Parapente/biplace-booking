{
  "name": "backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dotenv": "dotenv -e ../../infra/.env",
    "dev": "pnpm dotenv -- ts-node-dev -r tsconfig-paths/register --respawn --transpile-only src/main.ts | pino-pretty",
    "start": "node dist/main.js",
    "build": "pnpm dotenv -- tsc -p tsconfig.json && tsc-alias",
    "test": "echo \"No tests configured\"",
    "prisma:generate": "pnpm dotenv -- pnpm exec prisma generate",
    "prisma:migrate": "pnpm dotenv -- pnpm exec prisma migrate dev",
    "prisma:deploy": "pnpm dotenv -- pnpm exec prisma migrate deploy",
    "start:prod": "pnpm prisma:generate && pnpm prisma:deploy && node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "10.0.0",
    "@nestjs/config": "3.0.0",
    "@nestjs/core": "10.0.0",
    "@nestjs/cqrs": "11.0.3",
    "@nestjs/platform-express": "10.0.0",
    "@prisma/client": "6.18.0",
    "auth0": "5.0.0",
    "class-transformer": "0.5.1",
    "class-validator": "0.14.0",
    "dotenv-cli": "11.0.0",
    "express": "4.18.2",
    "nestjs-pino": "4.4.1",
    "newrelic": "13.6.2",
    "pino": "9.0.0",
    "pino-pretty": "13.1.2",
    "prisma": "^6.18.0",
    "reflect-metadata": "0.1.13"
  },
  "devDependencies": {
    "@types/express": "4.17.21",
    "ts-node-dev": "2.0.0",
    "tsc-alias": "1.8.16",
    "tsconfig-paths": "4.2.0",
    "typescript": "5.9.3"
  }
}
